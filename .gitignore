# Local tooling — not part of the live site
.claude/
.DS_Store
Thumbs.db
desktop.ini

# Dev-only scripts, notes, and harnesses (do not upload these)
_dev/
__pycache__/
*.py
*.pyc

# Scratch / preview / screenshot-harness output (never ship)
_*.html
*__*.html
*_p2.html
_host_*
_iso_*

# Internal tile generator (not linked from any page).
# Delete this line if you actually want it published.
tiles.html
